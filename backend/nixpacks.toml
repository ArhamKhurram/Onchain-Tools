# Use when Railway service root directory is set to /backend

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = [
  "npm run build",
  "npx playwright install chromium"
]

[start]
cmd = "npm run start"
